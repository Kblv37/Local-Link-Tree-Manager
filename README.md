# Local Link Tree Manager

Локальное расширение для браузера, позволяющее хранить, структурировать и быстро находить ссылки в виде древовидной структуры папок — **без облаков и сторонних сервисов**. Всё хранится локально в браузере

---

##  Основные возможности

-  Древовидная структура папок и ссылок  
-  Быстрый поиск в popup (моментальный переход по нажатию)  
-  Настраиваемая глобальная горячая клавиша для открытия popup  
-  Перемещение папок и ссылок с помощью кнопок (без drag & drop)  
-  Cut / Paste элементов дерева  
-  Ручное сохранение (без автосохранения файла)  
-  Экспорт в читаемый TXT (структурированный формат)  
-  Импорт из TXT (восстановление структуры)  
-  Основное хранилище — `chrome.storage.local`  
-  Оптимизированное открытие popup для Manifest V3

---

##  Архитектура

- **Popup** — быстрый поиск и переход по ссылкам. Минимум логики при загрузке для скорости.  
- **Options (страница настроек)** — редактирование дерева, перемещение элементов, импорт/экспорт.  
- **chrome.storage.local** — основное хранилище данных.  
- **TXT-файл** — ручной экспорт/импорт для резервного копирования и переноса.

---

##  Формат TXT (пример)

Файл читабелен и легко редактируется вручную:
=== LINK TREE ===

📁 Development
🔗 GitHub
https://github.com

🔗 StackOverflow
https://stackoverflow.com

📁 Personal
🔗 Notes
https://example.com/notes


**Примечание:** при импорте парсер связывает заголовок ссылки (строка с `🔗`) с последующей строкой URL (начинающейся с `http`/`https`).

---

##  Установка

1. Склонируйте репозиторий:  
   `git clone https://github.com/Kblv37/Local-Link-Tree-Manager.git`  
2. Откройте Chrome/Edge: `chrome://extensions/`  
3. Включите **Режим разработчика**  
4. Нажмите **Load unpacked** и выберите папку проекта

---

##  Горячие клавиши

Назначение горячих клавиш и возможные проблемы:

- По умолчанию комбинация задаётся в `manifest.json` (и/или через `chrome://extensions/shortcuts`).  
- Если расширение не использует `background` (service worker), то команда должна называться `_execute_action`, чтобы Chrome автоматически открыл `action.default_popup`.  
- В `chrome://extensions/shortcuts` можно изменить комбинацию вручную.

---

##  Как пользоваться

- Открой popup горячей клавишей → начните вводить для поиска → Enter — переход по ссылке.  
- В Options: редактируйте папки/ссылки, используйте кнопки ⬆⬇ для перемещения, ✂ для вырезания и ⤓ для вставки.  
- Сохранение: нажмите **Сохранить** — данные сохраняются в `chrome.storage.local`.  
- Экспорт: нажмите **Экспорт** — скачивается TXT.  
- Импорт: нажмите **Импорт**, выберите TXT — структура загрузится в редактор.

---

##  Последние обновления (v1.2.0)

- Удалён Drag & Drop (заменён на кнопочное перемещение)  
- Добавлено перемещение папок и ссылок кнопками ⬆⬇  
- Экспорт переведён с JSON на TXT  
- Импорт теперь работает из TXT-файла с корректным парсером  
- Убрана автоматическая запись файла при сохранении (ручной экспорт)  
- Оптимизировано открытие popup через горячую клавишу (MV3)  
- Улучшена обработка команд (`chrome.commands`) и стабильность работы в разных раскладках, горячяя клавиша для вызова расширения `Alt + Q`
- Улучшена структура хранения данных и стабильность импорта/экспорта

---

##  Отладка проблем с горячими клавишами

Если горячая клавиша не работает:

1. Открой `chrome://extensions/shortcuts` — убедись, что комбинация назначена.  
2. Перезагрузи расширение на `chrome://extensions` (кнопка Reload).  
3. Если используешь `background` (service worker) — открой инспектор Service Worker и проверь консоль на ошибки.  
4. Попробуй временно сменить комбинацию на `Ctrl+Shift+Space` — это помогает исключить системные конфликты.  
5. Убедись, что команда в manifest совпадает с тем, что слушает `background.js` (если он есть).

---

##  Версия 

v1.2.0